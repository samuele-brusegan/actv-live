# Database (GTFS data)
DB_HOST=localhost
DB_USER=your_db_user
DB_PASS=your_db_password
DB_NAME=actv_live

# Password per l'area /admin/* (login form). CAMBIARLA in produzione.
ADMIN_PASSWORD=changeme
